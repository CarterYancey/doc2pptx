[] Allow users to specify the generated ppt name

[] Add settings to the UI to change the performance of the LLM (perhaps some default 'modes' that affect temperature/top-p behind the scenes, and maybe even change the default prompt)

[] Extract images from the source material and insert them into the ppt when applicable.

[] Using a template does not cause default background images to apply to normal content slides (deck title and section slides seem to work). Slide markings (like "proprietary information" warnings) are not copied. 

[] If a template provides not only style formatting but also content guide, the LLM should try to follow that when it creates the markdown slide outline.

[] Create more sophisticated and interesting slides. More than just boring bullets. (Perhaps use the Claude PPT Skill as inspiration? It is source available, not open source, and requires agents).
